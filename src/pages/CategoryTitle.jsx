const CategoryTitle = ({ title }) => {
  return (
    <div className="text-center mb-5 sm:mb-3">
      <h2 className="text-xl sm:text-lg">{title}</h2>
    </div>
  );
};

export default CategoryTitle;
